apply plugin: 'java-library'

description 'Present RPC core library'

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

dependencies {
    api deps.wire.runtime
    implementation deps.gson
    implementation deps.wire.gson
}
